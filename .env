# Frontend build/runtime vars
VITE_API_BASE=/api
# For dev, if you have a local API server, uncomment and set:
# VITE_API_PROXY_URL=http://localhost:8000

# Remove backend-only vars; keeping uploads config if used by frontend
UPLOAD_MAX_SIZE=10mb
ENABLE_AWSTATS=false